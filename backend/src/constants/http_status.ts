//backend/src/constants/http_status.ts
// Constants for HTTP status codes commonly used in API responses
export const HTTP_BAD_REQUEST = 400;       // Represents a client error where the request cannot be processed due to invalid data or formatting.
export const HTTP_UNAUTHORIZED = 401;      // Represents an unauthorized request, typically requiring authentication or lacking valid credentials.
